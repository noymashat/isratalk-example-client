{
    "rules": {
    		".read": "auth != null",
        ".write": "auth != null",
        "customers": {
        	".indexOn": ["uid", "phoneNumber"]
        },
        "transactions":{
        	".indexOn": ["agent", "customerKey"]
        },
        "users":{
          ".indexOn": ["username", "email"],
        },

    }
}

